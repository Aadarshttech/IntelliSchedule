const ConflictsView = {
    render() {
        return `
            <div class="card">
                <h3>Conflict Analysis</h3>
                <p>No conflicts detected. The CP-SAT solver ensures all hard constraints are met.</p>
            </div>
        `;
    },
    init() {}
};
